/*const person = {
  name: "jhon",
  age: 30,
  weight: 88.6,
  isAdmin: true,
};

console.log(`${person.name} tem ${person.age} anos`);
*/

const persona = {
  name: "joao",
  age: 20,
  wheight: 75,
  isAdmin: true,
};

console.log(`${persona.name} tem ${persona.age} anos`);
