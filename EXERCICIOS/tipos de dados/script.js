// Declarar uma variavel de nome weight

//var weight;

// Que tipo de dado é a variavel acima?
//Number
//console.log(typeof weight);

/* Declare uma variavel e atribua valores para cada um dos dados:
    *name: string
    age: number (integer)
    stars: Number (float)
    *isSubscribed: boolean
*/

//let name = "João";
//let age = 25;
//let stars = 29.8;
//let insSubiscriber = true;

//console.log(insSubiscriber, age);

let student = {
  name: "João",
  age: 20,
  weight: 78.5,
};

let student2 = {
  name: "Michael",
  age: 19,
  wheight: 86.4,
};
/*console.log(`${student.name} de idade ${student.age} pesa ${student.weight}`);
 */

let students = [student, student2];

console.log(students);
